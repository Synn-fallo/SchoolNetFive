// /home/project/app/(app)/enseignant/notes.tsx
// Route pour "Évaluations & Notes"

import EnseignantNotesView from '@/components/notes/EnseignantNotesView';

export default function NotesRoute() {
  return <EnseignantNotesView />;
}